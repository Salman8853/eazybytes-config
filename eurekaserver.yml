server:
  port: 8070
eureka:
  instance:
    hostname: locahost
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultsZone: http://${eureka.instance.hostname}:${server.port}/eureka/
